# Crisp — Static Site

Marketing site for **Crisp**, the 100%-offline AI video & photo upscaler + auto-editor for Mac. Served at https://crispvideo.app.

Edit `index.html` and push to `main` — GitHub Pages auto-deploys to crispvideo.app.

Serve locally:

```bash
python3 -m http.server 5601
```
